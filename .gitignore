node_modules
.env
.env.local
dist
dist-server
.DS_Store
# Generated by build — Vercel uses the build-time artifact, not a committed file
api/index.js
# Vercel Build Output API — generated by build script, not committed
.vercel/output
server/public
vite.config.ts.*
*.tar.gz

# Vite prebundle cache (do not commit)
.vite-cache/
node_modules/.vite/

# Nested clone / accidental repo inside this project
web-nova-crew-ai-company/

# Local editor settings (optional)
.vscode/

# Runtime share link store (project root only)
/data/